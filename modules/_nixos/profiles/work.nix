# Work profile adjustments.
# Activated when my.profile == "work" or imported explicitly by a host.
{...}: {
  # Placeholder — add work-specific NixOS settings here
  # (e.g. VPN, corporate CA certs, stricter firewall).
}
